# Production environment — consumed by Vite at build time on Vercel
# Set VITE_API_URL to your actual Render backend URL in the Vercel dashboard
# (Project → Settings → Environment Variables)
VITE_API_URL=https://ecoledger-backend.onrender.com
VITE_MOCK_API=false
VITE_APP_NAME=EcoLedger
